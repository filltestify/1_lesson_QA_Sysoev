# Кухня у Дома — QA-сессия (Задание 4)

Итоговый отчёт по домашнему заданию курса (Занятие 1, направление
QA). Готовый прототип сервиса доставки еды прогнан через
дисциплинированный QA-цикл: контекст → спецификация → тест-план →
проверка → баг-репорты → независимый аудит. Ход работы, промпты и
решения — в `sessions/`; этот файл — краткий свод результата.

## Запуск

```bash
# установка зависимостей
# нет — проект без npm и без сборки

# запуск приложения
python3 -m http.server 8765
# открыть http://localhost:8765/ — другой порт/адрес не используется

# автотест логики
node app.logic.test.mjs
# ожидаемый вывод: OK: all logic checks passed
```

Requirements: Python 3 (только для локального сервера) и Node.js
(только для `app.logic.test.mjs`) — оба не входят в рантайм самого
приложения, оно чистый `HTML/CSS/vanilla JS`.

## Что это за проект

Фронтенд-прототип без бэкенда: меню одного ресторана → корзина →
оформление заказа → подтверждение → история заказов (хранится в
`localStorage`). Приложение было собрано в отдельной, уже закрытой
сессии разработки — по сценарию Задания 4 QA-агент к моменту начала
работы получает готовое приложение и историю его создания не видит.
Мою роль в этом проекте описывает `AGENTS.md`: тестировщик, который не
пишет и не правит код приложения ни при каких обстоятельствах.

## Как шла работа (полный цикл)

| Шаг | Файл | Итог |
|---|---|---|
| Контекст | `AGENTS.md` | Часть II заполнена под этот проект: стек, команды, запреты, когда останавливаться и спрашивать |
| Спецификация QA-сессии | `SPEC.md` | Собрана диалогом «вопрос за вопросом» (скилл `brainstorming`) — объект тестирования, 4 зоны риска, критерии приёмки, открытые вопросы |
| Тест-план | `test-plan.md` | 13 пунктов (12 исходных + 1 по итогам аудита), с перевесом на зоны формы и истории заказов |
| Баг-репорты | `bug-reports.md` | 5 найденных дефектов с доказательствами (скриншоты, консоль, автоматизированный сценарий) |
| Проверка/находки | `qa-notes.md` | Список неоднозначностей и объяснённый пустой список расхождений |
| Независимый аудит | `sessions/session-2.md` | Отдельный агент без контекста сессии 1 перепроверил находки живьём |
| Журнал | `sessions/session-1.md`, `sessions/session-2.md`, `sessions/STATE.md`, `sessions/TOOLS.md` | Промпты дословно, размышления, тупики, инструменты |

## Найденные баги

Полные баг-репорты с шагами воспроизведения и доказательствами —
`bug-reports.md`. Скриншоты — `screenshots/`.

| № | Заголовок | Severity | Зона |
|---|---|---|---|
| 1 | Поле «Имя» не экранируется — HTML/JS-инъекция на экране подтверждения | Критично | Валидация формы |
| 2 | Некорректные данные в `localStorage["orders"]` роняют переход на «Мои заказы» необработанным исключением | Критично | История заказов |
| 3 | Отправка формы с опустошённой корзиной молча возвращает в меню без сообщения | Не критично | Валидация формы |
| 4 | Данные формы (имя/адрес/телефон) не очищаются между визитами — риск отправить заказ со старыми чужими данными | Критично | Валидация формы |
| 5 | Тексты ошибок валидации виснут на экране без новой попытки отправки | Не критично | Валидация формы |

Баги 1–3 найдены при выполнении исходного тест-плана. Баги 4 и 5 —
при перепроверке находок независимого аудита (баг 4 подтверждён не
только по шагам, но и «на живом тестировщике»: при ручной проверке я
дважды подряд случайно отправил реальные заказы со старыми данными —
это зафиксировано в самом баг-репорте как часть доказательства, а не
скрыто).

## Сравнение: наивный результат vs результат по спеке

Наивный прогон в классическом смысле (короткий промпт → сразу код) в
этом проекте не мой — по сценарию Задания 4 такой прогон и его
закрытая сессия предшествуют QA и не видны тестировщику. Он всё же
здесь есть по факту: сам прототип — и есть тот наивный результат,
против которого работал дисциплинированный QA-процесс. Сравнение
получилось предметным:

- **Наивный результат** (что видно при беглом клике по интерфейсу):
  приложение выглядит рабочим — меню открывается, товары добавляются в
  корзину, заказ оформляется, подтверждение показывается, история
  заказов сохраняется. На поверхностный взгляд — готовый продукт.
- **После спеки и тест-плана**: систематический разбор по зонам риска
  (не «потыкать всё по чуть-чуть», а глубоко покопать в 1–2 зонах)
  вскрыл 5 реальных дефектов, два из них критичные — выполнение
  произвольного кода через поле формы (XSS) и падение целого экрана
  «Мои заказы» на некорректных данных. Ни один из них не заметен при
  обычном ручном прохождении happy path — оба требуют осознанно
  негативного/граничного ввода, которого «наивная» ручная проверка не
  делает.
- **После независимого аудита**: свежий агент без контекста не только
  подтвердил находки живой переисполненной проверкой (не чтением), но
  нашёл ещё один функциональный баг (персистентность старых данных
  формы — риск реальной отправки заказа не по адресу) и один пробел
  покрытия (переход слотов доставки через полночь был обещан спекой,
  но пропущен в исходном плане).

Конкретный вывод: без спеки и тест-плана заявленных багов 1, 2 и 4 в
отчёте не было бы вовсе — они не видны без целенаправленного
негативного/граничного ввода. Аудит добавил ещё один слой: то, что
пропустил первый проход (Баг 5 и пробел с полночью), поймал второй,
независимый.

## Что не сделано / сознательно упрощено

- **Повторная проверка после фикса бага** — не выполнена. Правка кода
  приложения запрещена QA-агенту `AGENTS.md`, фиксов не было и не
  должно было быть в рамках этой роли. Критерий ждёт отдельной будущей
  сессии, где кто-то другой исправит хотя бы один из 5 багов.
- **Кроссбраузерность** — тестировалось только в Chrome через
  `claude-in-chrome`. Второй браузерный движок (Safari/Firefox) не
  проверялся — в сессии не было для него автоматизации, а
  устанавливать новый инструмент (например, Playwright MCP) без
  отдельного вопроса запрещено правилами проекта. Обозначено как
  задел на будущее.
- **Accessibility** (клавиатурная навигация, видимость фокуса) —
  сознательно вне рамок этой сессии: не требовалось заданием, не
  укладывается в выбранную таксономию кластеров багов, отдельный
  QA-раунд на будущее.
- **Мобильная ширина 375px** — проверена не полностью. Через
  `claude-in-chrome` окно Chrome в этой среде физически не сужается
  меньше 606px (проверено, не единичный случай). Испробован обходной
  путь через CSS `zoom` — не сработал, медиа-запросы Chrome считаются
  от физического viewport, а не от CSS-зума. Реально подтверждено:
  768px и 606px — вживую скриншотами на всех 5 экранах; поведение
  <420px — только чтением синтаксически верного CSS-правила в
  `style.css`, без подтверждения фактического рендера. Граница честно
  зафиксирована в `qa-notes.md`, а не выдана за «пройдено».
- **Наивный прогон продукта** — не выполнялся мной напрямую (см. выше)
  — по сценарию Задания 4 эта сессия закрыта заранее.

## Допущения и ограничения

- Тестовые данные — реалистичные российские (имя, адрес, телефон), без
  проработки персоны или демографии продукта.
- `localStorage` браузера доступен и не ограничен приватным/инкогнито-
  режимом на машине тестировщика.
- Системные часы машины тестировщика не менялись ни разу — там, где
  нужно было проверить конкретное время (границы получаса, переход
  через полночь), использовался прямой вызов `generateDeliverySlots` с
  явно заданным `Date`, а не смена системных часов ОС (это запрещённое
  действие).
- Сервер запускается строго `python3 -m http.server 8765`, приложение
  живёт только на `http://localhost:8765/`.
- Версия Chrome, использованная в тестировании, — 151.0.0.0 (macOS,
  Darwin 25.5.0), зафиксирована в окружении каждого баг-репорта.
- Полный список открытых вопросов (пробелы вокруг полей, повторная
  отправка формы, часы работы заведения, политика экранирования) — в
  `qa-notes.md`, каждый — с явной отметкой, зафиксирован ли по нему уже
  факт поведения или нет.

## Независимый аудит

Проведён отдельным `general-purpose`-агентом без контекста QA-сессии —
только самодостаточный бриф и код проекта. Аудит независимо
переисполнил (не просто прочитал) два критичных бага и получил
идентичные посимвольно консольные трассировки, нашёл фактическую
неточность в одном баг-репорте, один пропуск покрытия относительно
спеки и один новый баг. Полный текст отчёта аудитора и разбор по
каждой находке — `sessions/session-2.md`. Ни с одной находкой аудита
не спорили по существу — разногласия были только по формулировке и
группировке.

## Структура проекта

```
index.html, app.js, logic.js, style.css   — приложение (не менялось в QA)
app.logic.test.mjs, package.json          — существующий автотест логики
AGENTS.md, CLAUDE.md                      — правила проекта (Часть II заполнена)
SPEC.md                                   — спецификация QA-сессии
test-plan.md                              — тест-план, 13 пунктов
bug-reports.md                            — 5 баг-репортов с доказательствами
qa-notes.md                               — неоднозначности, расхождения, итог аудита
screenshots/                              — доказательства к баг-репортам
sessions/                                 — журнал: session-1.md, session-2.md, STATE.md, TOOLS.md
```

## Честно: чего в этом отчёте не было

Отчёт с идеально гладким ходом работы выглядел бы придуманным. Что
реально было: неверно понятая последовательность шагов на этапе
спецификации (написал чек-листы до того, как их попросили, пришлось
откатиться к вопросам); передумывание пользователя по выбору
браузерного окружения прямо в диалоге; техническое ограничение среды,
не позволившее закрыть один пункт тест-плана честно на заявленной
ширине; собственная ошибка при автоматизации, которая случайно
отправила два реальных заказа со старыми данными — и именно она стала
лучшим доказательством одного из багов. Все эти моменты зафиксированы
в `sessions/session-1.md` и `sessions/session-2.md` как есть, без
приглаживания.
