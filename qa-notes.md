# QA-заметки — неоднозначности и расхождения

## Список неоднозначностей

Ожидаемое поведение нигде не задано — трактовку не выбираем сами,
фиксируем вопрос. Там, где тест-план успел зафиксировать фактическое
(а не ожидаемое) поведение, это добавлено пометкой «Факт» — сам вопрос
от этого не закрывается, окончательное решение всё ещё за заказчиком.

1. Пробелы по краям поля (имя/адрес) — обрезаются ли перед
   сохранением/показом, или сохраняются как есть? *(не проверялось
   отдельным пунктом тест-плана — осталось открытым без факта)*
2. Что должно происходить при испорченном/структурно некорректном
   `localStorage`? **Факт (тест-план, п. 5–6):** сейчас — необработанное
   исключение, экран истории не открывается, кнопка навигации
   визуально «залипает» активной без реального перехода. Заведено как
   **Баг 2** в `bug-reports.md`, поскольку такое поведение не может быть
   осознанным выбором ни при какой трактовке вопроса.
3. Повторная отправка формы оформления (два быстрых клика) — создание
   дублирующего заказа: разрешить, блокировать, или идемпотентность?
   *(не проверялось отдельным пунктом — осталось открытым без факта)*
4. Персистентность корзины между перезагрузками страницы — должна
   сохраняться (как история заказов) или намеренно эфемерна?
   **Факт (тест-план, п. 8):** сейчас корзина не персистентна, любая
   перезагрузка страницы обнуляет её. В баг не заведено — решение о
   том, ожидаемо это или нет, остаётся за заказчиком.
5. Слоты доставки генерируются круглосуточно без учёта часов работы
   заведения — это осознанное решение или недосмотр? *(не
   перепроверялось отдельно — фактическое поведение уже задокументировано
   кодом и существующими unit-тестами, вопрос о намерении открыт)*
6. Политика экранирования пользовательского ввода (имя/адрес/телефон/
   комментарий) в `innerHTML` на экране подтверждения — нигде не
   описана. **Факт (тест-план, п. 3):** экранирования нет, инъекция
   выполняется. Заведено как **Баг 1** — независимо от того, как решить
   этот открытый вопрос в будущем, текущее поведение (выполнение
   произвольного кода) не может быть приемлемым ни при одной трактовке.

## Список расхождений

Пусто — намеренно, с объяснением, а не просмотрено. «Расхождение» по
определению `AGENTS.md` — это случай, где в закрытой сессии разработки
было явно заявлено «готово», а QA-проверка это опровергла. У этой
QA-сессии нет доступа к стенограмме сессии разработки (она закрыта
до начала QA, как и требует Задание 4) — то есть неизвестно, что
именно разработчик считал «готовым», а что — сознательно упрощённым.
Поэтому 5 найденных багов в `bug-reports.md` (3 из тест-плана, 2 —
из перепроверки в ходе независимого аудита) заведены как баги, а не
натянуты на формат «расхождения»: подгонка под этот раздел без
реального заявления «готово» была бы нечестной записью.

## Техническое ограничение среды — узкая ширина (375px)

Пункт 12 тест-плана выполнен частично. Через `claude-in-chrome` окно
Chrome физически не сужается меньше **606px** (проверено запросами на
768/375/320px — везде `window.innerWidth` остаётся 606). Испробован
предложенный обходной путь — CSS `zoom: 200%` на `<html>` — тоже не
сработал: `matchMedia('(max-width: 420px)').matches` остаётся `false`
даже при таком зуме, то есть медиа-запрос в Chrome считается от
физического viewport окна, а не от CSS zoom.

Что реально подтверждено:
- **768px и 606px** — проверены вживую скриншотами на всех 5 экранах
  (меню, корзина, оформление, подтверждение, история): вёрстка не
  ломается, горизонтальной прокрутки нет.
- **Поведение ниже 420px** — подтверждено только чтением кода: в
  `style.css` единственный breakpoint по ширине —
  `@media (max-width: 420px) { .menu-item__name { flex-basis: 100%; min-width: 100%; } }`
  (проверено `grep`, строки 564–569). Правило синтаксически корректно
  и нацелено на карточки меню, но его фактический рендер в браузере
  **не подтверждён** — DevTools device-emulation сознательно не
  пробовали (риск нестабильной автоматизации ради одного пункта плана).

Граница честно зафиксирована: 768/606px — визуально подтверждено,
<420px — подтверждён только код, не рендер.

## Независимый аудит (сессия 2) — итог

Полный отчёт аудитора и разбор по каждой находке — в
`sessions/session-2.md`. Коротко:

- Баги 1 и 2 — независимо переисполнены живьём, консольные трассировки
  совпали с исходным отчётом посимвольно. Без замечаний.
- Баг 2 — аудит дополнительно проверил третий вариант порчи данных
  (`'null'`), тоже падает тем же способом — подтверждает решение не
  дробить баг-репорт по вариантам.
- Баг 3 — найдена и исправлена неточность формулировки («данные
  теряются» → на деле поля не очищаются, но и не сохраняются
  осмысленно; сама проблема — молчаливый редирект без сообщения).
- Найдены и заведены отдельными багами (не слиты, разная severity):
  **Баг 4** (Критично) — старые ФИО/адрес молча предзаполняют форму
  при следующем визите, реальный риск отправить новый заказ по старому
  адресу; **Баг 5** (Не критично) — тексты ошибок валидации виснут на
  экране до новой попытки отправки, чисто косметика.
- Найден пробел покрытия: `SPEC.md` обещал проверку перехода слотов
  доставки через полночь, тест-план это пропустил — закрыто пунктом 13
  в `test-plan.md`, код при проверке оказался корректным (не баг).
- Проверка соответствия `AGENTS.md` (код не тронут во время QA) —
  подтверждена по таймстампам файлов, без замечаний.
- `sessions/session-2.md` на момент отчёта аудитора содержал
  незаполненный плейсхолдер вместо вердикта — дозаполнено по
  результатам этого аудита.
